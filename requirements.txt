# ============================================================
#  qbot 核心依赖（新手只需装这些，Windows 上基本不会出错）
#  安装：  python -m pip install -r requirements.txt
# ============================================================

# 核心计算
numpy>=1.26
pandas>=2.0

# 数据源
ccxt>=4.2              # 加密货币交易所统一接口（含 OKX）
requests>=2.28         # A股腾讯行情 + 信号通知推送

# 可视化面板
fastapi>=0.110
uvicorn[standard]>=0.29

# 配置
pyyaml>=6.0

# ============================================================
#  可选依赖（不装也能用全部核心功能，需要时再单独装）
#    列出全部A股代码：      pip install akshare
#    OKX 官方SDK(可选)：    pip install python-okx
#  注：A股历史行情已用腾讯源(requests)，无需 akshare；
#      面板图表用浏览器端 Plotly，无需 python 版 plotly。
# ============================================================
